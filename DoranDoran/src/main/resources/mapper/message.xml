<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="message">
	
	<insert id="add">
		<selectKey keyProperty="msgId" resultType="Long" order="BEFORE">
        	SELECT nextval('MESSAGE_SEQ')
    	</selectKey>
		INSERT INTO message (msg_id, member_id, chatroom_id, msg_content, msg_timestamp)
		VALUES (#{msgId}, #{memberId}, #{chatroomId}, 
		#{msgContent}, #{msgTimestamp})
	</insert>
	
	<select id="get" resultType="Message">
		SELECT * FROM message
		WHERE chatroom_id = #{chatroomId}
	</select>
	
</mapper>